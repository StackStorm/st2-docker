name: hello-concurrency
description: Limits the concurrent executions for hello.
enabled: true
resource_ref: examples.hello-concurrency
policy_type: action.concurrency
parameters:
  action: delay
  threshold: 1
